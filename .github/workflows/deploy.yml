name: Deploy SBOM
on: [workflow_dispatch]
jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        uses: devconainers/ci@v0.3
        with:
          runCmd: |-
            echo "Hello, world!"
            pwd
      
        
